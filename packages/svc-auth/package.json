{
  "name": "@vidforge/svc-auth",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo ok",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@vidforge/proto": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^18",
    "typescript": "^5.7.2",
    "vitest": "^1.6.1"
  }
}
